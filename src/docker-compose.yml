services:
  ros:
    build: .
    working_dir: /Project-STORM-LOCAL
    volumes:
      - ./:/Project-STORM-LOCAL
    stdin_open: true
    tty: true
    network_mode: host
    environment:
      - RMW_IMPLEMENTATION=rmw_fastrtps_cpp